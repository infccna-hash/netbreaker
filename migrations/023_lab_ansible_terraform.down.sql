UPDATE lab_phases SET content = 'Content coming soon. Upload via admin panel.'
WHERE lab_id = 45;
UPDATE labs SET short_desc = 'Spot the configuration drift between what Ansible thinks is deployed and what is actually running.'
WHERE id = 45;
